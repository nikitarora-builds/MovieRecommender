# MovieRecommender
Hybrid movie recommender combining SVD matrix factorization (collaborative filtering) with sentence-transformer semantic search over FAISS. CF learns user taste from 100K ratings; semantic layer enables natural-language queries like "dark psychological thriller." A weighted blend of both handles cold-start and improves catalog coverage.
